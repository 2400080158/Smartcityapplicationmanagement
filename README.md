
  # Specify Required Fields

  This is a code bundle for Specify Required Fields. The original project is available at https://www.figma.com/design/gtCchf6bryg4X4m2TOSgF6/Specify-Required-Fields.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  